# Test Web Socket Chat

Download this folder, navigate it, then install with:

```bash
npm install .
```

Then run the server
```bash
node ./server.js
```

Then visit the webserver that starts.

You should see a chat box. Open 2 browsers to test the chat.

You should be able to restart the server and refresh the page and messages should persist.